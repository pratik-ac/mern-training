use("kec-crud");

// db.movies.find().count(); // ! count give number of movies in database

// ? delete movie whose name is "Glee"
// db.movies.deleteOne({ name: "Glee" });
// db.movies.find({ name: "Glee" });

// db.movies.deleteMany({}); // ! delete whole database

// TODO : optimization
// ? index, queryExecutionTime,co-ordinates
