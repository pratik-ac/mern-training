const buttonStyle = {
  padding: "1rem",
  background: "black",
  color: "white",
  border: "none",
  cursor: "pointer",
};

const Button = () => {
  return <button style={buttonStyle}>send me</button>;
};

export default Button;
