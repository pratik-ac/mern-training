// ? truthy and falsy values

// ! falsy values
// ? when these values come in condition, they are equivalent to false value
// false, null, undefined, 0, -0, NaN, ""

// ! truthy values
// ? all values except falsy values are truthy values

let condition = 1;
console.log(condition ? "Truthy" : "Falsy");
